name: challenge

on: [push, pull_request]

jobs:
  run-on-ubuntu:
    name: Print Date in ubuntu-latest
    runs-on: ubuntu-latest
    steps:
      - run: date
  run-on-macos:
    name: Print Date in macos-latest
    runs-on: macos-latest
    steps:
      -  run: date
  run-on-dependency-stage:
    name: Print Date in Dependency stage
    needs: [run-on-ubuntu, run-on-macos]
    runs-on: ubuntu-latest
    steps:
      -  run: date